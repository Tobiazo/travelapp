

const LoginButton = ({children}) => {

    const handleClick = () => {
        return <></>
    }

    return (

        <button onClick={handleClick}>
            {children}
        </button>
    );
};

export default LoginButton;